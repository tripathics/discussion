// writing strings using using using puts //

/* puts() function is a function declered in <stdio.h> library and is used 
to write a newline charecter after string to the output screen .*/

#include <stdio.h>

int main()
{
    char *ptr = "Hello brother";

    puts(ptr);

    return 0;
}